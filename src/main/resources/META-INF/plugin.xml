<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.postmanopenapidriftcompanion</id>
    <name>Postman OpenAPI Drift Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on a Postman collection (<code>*.postman_collection.json</code>)
        endpoint whose method+path no longer exists in the project's
        OpenAPI/Swagger spec. Collections and specs drift apart
        silently: a collection is hand-edited or an endpoint gets
        renamed/removed in the spec, and nothing tells the team the
        collection now exercises a path that doesn't exist -- usually
        caught only when a request in the collection starts returning
        404.</p>
        <p><b>100% static analysis</b> -- reads the Postman collection's
        real <code>item[].request.url</code> via the bundled JSON
        plugin's PSI (Postman Collection Format v2.1.0's own schema),
        and the OpenAPI/Swagger spec's <code>paths:</code> via a plain
        text scan (works for either YAML or JSON specs, no YAML plugin
        dependency). Path placeholders (<code>{id}</code>,
        <code>:id</code>, <code>{{id}}</code>) are matched as wildcards
        on both sides.</p>
        <p>Stated honestly: v0.1 looks for a conventionally-named spec
        file (<code>openapi.yaml/.yml/.json</code>,
        <code>swagger.yaml/.yml/.json</code>) walking up from the
        collection's own directory -- a spec filed elsewhere in the
        project, or referenced only via <code>$ref</code>, isn't found,
        and the inspection stays silent rather than guessing.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.json</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language="JSON"
            groupName="Postman OpenAPI Drift Companion"
            displayName="Postman endpoint missing from OpenAPI spec"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.postmanopenapidriftcompanion.inspection.PostmanEndpointDriftInspection"/>
        <notificationGroup id="Postman OpenAPI Drift Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
